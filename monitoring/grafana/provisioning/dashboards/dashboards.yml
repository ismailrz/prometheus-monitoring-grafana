apiVersion: 1

providers:
  - name:            'default'
    orgId:           1
    folder:          'Prometheus Stack'
    folderUid:       'prometheus-stack'
    type:            file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates:  true
    options:
      path: /var/lib/grafana/dashboards
